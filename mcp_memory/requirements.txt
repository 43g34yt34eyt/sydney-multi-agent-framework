# Phase 3 Dual Memory Architecture Dependencies
# Core database and memory systems

# PostgreSQL adapter
psycopg2-binary>=2.9.0

# NumPy for embedding operations
numpy>=1.21.0

# JSON handling and data processing
# (Built-in json and sqlite3 modules used)

# Optional: Better embedding generation (uncomment if needed)
# sentence-transformers>=2.2.0
# openai>=1.0.0

# Optional: Enhanced NLP for entity extraction (uncomment if needed) 
# spacy>=3.4.0
# nltk>=3.7

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Logging and monitoring (built-in logging used, but these could enhance)
# structlog>=22.0.0
# prometheus-client>=0.15.0

# Performance monitoring
# psutil>=5.9.0

# Note: This system is designed to use mostly built-in Python libraries
# for maximum compatibility and minimal dependency overhead.
# Uncomment optional dependencies as needed for production features.